# Style Guide

- Function and variable names must use `snake_case`, never `camelCase`.
- Every function must have a docstring describing what it does.
- Avoid bare `except:` clauses; always catch a specific exception type.
